---
phase: 03-testing-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/keyboard.rs
  - src/server.rs
  - src/socket.io.min.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
  - TEST-08
  - TEST-09
  - TEST-10

must_haves:
  truths:
    - "queue_select_all returns Paused when disabled (unit test coverage)"
    - "POST /api/commands without Authorization returns 401 (integration test)"
    - "POST /api/commands with wrong token returns 401 (integration test)"
    - "GET /api/history with wrong token returns 401 (integration test)"
    - "POST /api/commands returns 429 when queue is full (E2E test)"
    - "POST /api/commands clear_pc_field produces SelectAll + Backspace commands (E2E test)"
    - "cargo test passes — all 82+ tests pass, no regressions"
    - "cargo clippy — clean, no new warnings"
    - "src/socket.io.min.js no longer exists (dead file removed)"
  artifacts:
    - path: "src/keyboard.rs"
      provides: "queue_select_all paused unit test"
      contains: "test_queue_select_all_returns_paused"
    - path: "src/server.rs"
      provides: "Integration tests for auth, full-queue 429, clear_pc_field E2E"
      contains: "test_e2e_commands_full_queue_returns_429"
    - path: "src/server.rs"
      provides: "clear_pc_field E2E verification"
      contains: "test_e2e_clear_pc_field"
  key_links:
    - from: "tests in src/server.rs tests module"
      to: "router / api routes"
      via: "test_router().oneshot()"
      pattern: "test_router"
    - from: "tests in src/server.rs e2e_tests module"
      to: "actual server"
      via: "spawn_server + TcpStream"
      pattern: "spawn_server"
---

<objective>
Complete missing test coverage for TEST-02 through TEST-10 and clean up dead socket.io.min.js file.

Purpose: 82 tests already pass but several TEST-* requirements have gaps in coverage. This plan fills those gaps: auth tests for /api/commands and /api/history with wrong token, full-queue 429 E2E test, clear_pc_field command verification E2E test, queue_select_all paused state unit test. Also removes the dead socket.io.min.js file left over from the Socket.IO era.

Output:
- src/keyboard.rs amended with queue_select_all paused unit test
- src/server.rs amended with 5-6 new tests (auth coverage, full-queue 429, clear_pc_field E2E)
- src/socket.io.min.js deleted
- cargo test all pass (82+), cargo clippy clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-core/01-04-SUMMARY.md
@.planning/phases/02-frontend-rewrite/02-01-SUMMARY.md
@.planning/phases/02-frontend-rewrite/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add queue_select_all paused unit test to keyboard.rs</name>
  <files>src/keyboard.rs</files>
  <action>
    In src/keyboard.rs, in the existing `#[cfg(test)] mod tests` section, add a new test `test_queue_select_all_returns_paused_when_disabled` following the pattern of the existing `test_queue_type_text_returns_paused_when_disabled` test (lines 316-320):

    - Create a `TestGuard` in a `let _guard` binding
    - Call `set_enabled(false)`
    - Assert `queue_select_all()` returns `CommandResult::Paused`

    The test should be added in the "disabled returns Paused" section, after `test_queue_enter_returns_paused_when_disabled` (around line 335). No `#[serial_test::serial]` needed — this matches the existing pattern of queue_*_paused tests which don't use serial_test because they don't interact with the global COMMAND_TX channel.
  </action>
  <verify>
    <automated>cargo test test_queue_select_all_returns_paused 2>&1 | grep -q "1 passed"</automated>
  </verify>
  <done>
    "queue_select_all returns Paused when disabled is verified by a unit test"
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing server integration and E2E tests</name>
  <files>src/server.rs</files>
  <action>
    Add the following tests to src/server.rs:

    **In the `mod tests` section (non-E2E tests using test_router()):**

    1. `test_api_commands_unauthorized_without_header` — POST /api/commands (type_text, "hello") with no `Authorization` header → expect 401.

    2. `test_api_commands_unauthorized_with_wrong_token` — POST /api/commands (type_text, "hello") with `Authorization: Bearer wrong` → expect 401.

    3. `test_api_history_unauthorized_with_wrong_token` — GET /api/history with `Authorization: Bearer wrong` → expect 401.

    Follow the existing pattern from `test_api_status_unauthorized_without_header` / `test_api_status_unauthorized_with_wrong_token`. These tests don't need `#[serial_test::serial]` because the auth middleware runs before any keyboard-modifying code executes. No `TestGuard` needed.

    4. New test in existing `add_to_history` section: `test_add_to_history_empty_after_clear` — verify `add_to_history` with only whitespace `"   "` returns false and does not add to queue. (Optional extra coverage — the existing `test_add_to_history_rejects_empty` already covers `""` and `"   "`, so this is just for completeness if the existing test doesn't already cover whitespace. Actually, the existing `test_add_to_history_rejects_empty` at line 271-276 already tests both empty string AND whitespace string "   ". No additional test needed here.)

    Revised plan: No new history tests needed — existing coverage is already complete per the gap analysis.

    **In the `mod e2e_tests` section (real server tests using spawn_server):**

    5. `test_e2e_commands_full_queue_returns_429`:
       - Use `#[serial_test::serial]`
       - Create `keyboard::TestGuard`
       - `set_enabled(true)`
       - Create a `mpsc::sync_channel::<KeyCommand>(1)` — capacity 1, keep receiver alive but never read from it
       - `guard.replace_command_tx(ttx)` — replace the global channel with our tiny one
       - `spawn_server(E2E_TOKEN)`, `wait_for_server`
       - First POST: `type_text`, "fill" → assert 202 (fills the 1-capacity slot)
       - Second POST: `type_text`, "overflow" → assert 429 (channel full)
       - `tx.send(())` to shut down

    6. `test_e2e_clear_pc_field_produces_select_all_and_backspace`:
       - Use `#[serial_test::serial]`
       - Pattern: copy `test_e2e_command_flow` structure
       - Create `keyboard::TestGuard`, `set_enabled(true)`
       - Create `mpsc::sync_channel::<KeyCommand>(16)`, replace_command_tx
       - Spawn server, wait
       - POST clear_pc_field: assert 202
       - `recv_cmd(&trx)` → assert `KeyCommand::SelectAll`
       - `recv_cmd(&trx)` → assert `KeyCommand::Backspace`
       - Shut down

    Place tests 1-3 in the `mod tests` section, near the existing auth tests (around line 285-308). Place tests 5-6 in the `mod e2e_tests` section, near the existing E2E tests (after line 551).
  </action>
  <verify>
    <automated>cd /Users/ben/workspace/bot/type-bridge-rs && cargo test test_api_commands_unauthorized 2>&1 | grep -q "2 passed" && cargo test test_api_history_unauthorized_with_wrong_token 2>&1 | grep -q "1 passed" && cargo test test_e2e_commands_full_queue_returns_429 2>&1 | grep -q "1 passed" && cargo test test_e2e_clear_pc_field 2>&1 | grep -q "1 passed"</automated>
  </verify>
  <done>
    "All missing integration tests added: commands auth (2 tests), history wrong token (1 test), full queue 429 (1 E2E test), clear_pc_field command verification (1 E2E test)"
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete dead socket.io.min.js, run full test suite and clippy</name>
  <files>src/socket.io.min.js</files>
  <action>
    1. Delete `src/socket.io.min.js` — this file is 49,951 bytes of dead weight. It is not referenced by any Rust code (`include_bytes!` or similar), not imported by the HTML (verified by `test_html_has_no_socket_io_script`), and not declared in `Cargo.toml`. It is a leftover from the pre-refactor Socket.IO era.

    Before deleting, confirm it is not referenced by any source file:
    ```
    rg 'socket\.io\.min\.js' src/           # Should return 0 non-test matches
    rg 'include_bytes.*socket' src/         # Should return 0 matches
    ```

    2. Run `cargo test` — all tests must pass (expect 82+ now).
    3. Run `cargo clippy` — must be clean, no new warnings.
  </action>
  <verify>
    <automated>
      cargo test 2>&1 | tail -5
      cargo clippy 2>&1
    </automated>
  </verify>
  <done>
    "src/socket.io.min.js deleted, cargo test all pass, cargo clippy clean"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | HTTP request from phone browser to axum server over LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Auth middleware | mitigate | New tests verify auth rejection on /api/commands and /api/history without token / with wrong token |
| T-03-02 | Information Disclosure | Dead socket.io.min.js | mitigate | Delete unused 50KB file containing Socket.IO library (supply chain artifact with no functional purpose) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this plan |
</threat_model>

<verification>
- [ ] Task 1: `cargo test test_queue_select_all_returns_paused` passes
- [ ] Task 2: All 5 new server tests pass individually
- [ ] Task 3: `src/socket.io.min.js` no longer exists, full `cargo test` suite passes, `cargo clippy` clean
</verification>

<success_criteria>
- `cargo test` passes: auth coverage complete (no token, wrong token, correct token for all 3 API routes), command coverage complete (type_text, enter, backspace, clear_pc_field verified via E2E), edge case coverage complete (paused 409, full 429, empty 400, >10K 413), history coverage complete (write, dedup, 30-cap)
- `src/socket.io.min.js` removed
- `cargo clippy` clean with no new warnings
</success_criteria>
