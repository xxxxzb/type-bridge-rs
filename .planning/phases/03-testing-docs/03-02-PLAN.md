---
phase: 03-testing-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03

must_haves:
  truths:
    - "README architecture diagram shows axum HTTP, no Socket.IO"
    - "README dependencies table no longer lists socketioxide"
    - "README button table distinguishes Clear text (local phone clear) from Clear PC field (remote dangerous action)"
    - "README Backspace description says single character Backspace, not Option/Ctrl+Backspace word delete"
    - "README '与 Python 原版区别' section has no WebSocket/Socket.IO references"
  artifacts:
    - path: "README.md"
      provides: "Accurate architecture documentation for HTTP-only refactor"
      contains: "TypeBridge (axum HTTP)"
    - path: "README.md"
      provides: "Correct button semantics table"
      contains: "Clear text"
    - path: "README.md"
      provides: "Updated Python comparison"
      does_not_contain: "WebSocket"
  key_links:
    - from: "README.md arch diagram"
      to: "Cargo.toml dependencies"
      via: "axum listed, socketioxide absent"
      pattern: "axum"
---

<objective>
Update README.md to reflect the HTTP-only architecture, corrected button semantics, and remove Socket.IO references from the Python comparison section.

Purpose: The README is still documenting the pre-refactor architecture. The architecture diagram shows `axum + Socket.IO`, the dependencies table lists `socketioxide`, the button table describes the old Backspace behavior (word delete with modifier key) and a single "Clear" button instead of the new "Clear text" vs "Clear PC field" distinction, and the "与 Python 原版区别" section mentions "tokio 驱动的 WebSocket" — all outdated after Phase 1 + Phase 2.

Output: README.md updated — arch diagram, deps table, button table, Python comparison all accurate for the HTTP-only architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-backend-core/01-04-SUMMARY.md
@.planning/phases/02-frontend-rewrite/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update architecture diagram, dependencies table, and Python comparison section</name>
  <files>README.md</files>
  <action>
    Edit README.md with these changes:

    **Architecture diagram** (under "## 原理" section, lines 31-33):
    Current: `手机浏览器 ──WiFi──▶ TypeBridge (axum + Socket.IO) ──▶ enigo + arboard ──▶ PC 当前焦点应用`
    Change to: `手机浏览器 ──WiFi──▶ TypeBridge (axum HTTP + Bearer token) ──▶ enigo + arboard ──▶ PC 当前焦点应用`

    **Dependencies table** (under "## 安装 → 依赖" section, lines 58-67):
    - Remove the `socketioxide` row entirely
    - Add a new row for `axum` if it doesn't already exist (it should already be there)
    - Verify the table is accurate for the current post-refactor Cargo.toml. Current dependencies per Cargo.toml: axum, tokio, serde, serde_json, enigo, arboard, tray-icon, muda, clap, tracing, tracing-subscriber, qrcode, winit, softbuffer, if-addrs, getrandom. The table should list these, not the old set.

    Rewrite the table rows to match current Cargo.toml:
    ```
    | Crate | 用途 |
    |---|---|
    | `axum` | HTTP 服务器 + 路由 |
    | `tower` | HTTP 中间件（测试用） |
    | `enigo` | 跨平台键盘模拟 |
    | `arboard` | 剪贴板读写（处理所有 Unicode） |
    | `tray-icon` | 系统托盘图标 |
    | `muda` | 托盘菜单 |
    | `clap` | 命令行参数 |
    | `tokio` | 异步运行时 |
    | `qrcode` | 二维码生成 |
    | `winit` + `softbuffer` | 二维码窗口渲染 |
    ```

    Note: adjust to match actual Cargo.toml. Omit tower if it's a dev-dependency (it is — used only in tests). Consider adding a note that tower is dev-only.

    **Python comparison section** (under "## 与 Python 原版的区别" section, lines 131-139):
    - Change line `- **异步 I/O** — tokio 驱动的 WebSocket，更低的资源占用` 
    - To: `- **异步 I/O** — tokio 驱动的 HTTP 服务器，更低的资源占用`
    - Verify no other WebSocket or Socket.IO phrases appear in this section.
    - The line `- **优雅关闭** — 退出时服务端发送关闭帧` — is this still accurate? The HTTP server doesn't send close frames. Change to: `- **优雅关闭** — 退出时用 oneshot channel 通知 HTTP 服务器关停` or similar. Actually, looking at main.rs, the server uses `tokio::select` with a shutdown signal. The "关闭帧" (close frame) is a WebSocket concept. Change to: `- **优雅关闭** — 收到退出信号后平滑停止 HTTP 服务器`.
  </action>
  <verify>
    <automated>
      grep -c "Socket.IO\|socketioxide\|WebSocket" README.md | xargs test 0 -eq
    </automated>
  </verify>
  <done>
    "README architecture diagram shows HTTP-only, deps table has no socketioxide, Python comparison has no WebSocket/Socket.IO references"
  </done>
</task>

<task type="auto">
  <name>Task 2: Update button table with correct button semantics</name>
  <files>README.md</files>
  <action>
    Replace the current button table (under "## 按钮" section, lines 97-103) with the correct 4-button table reflecting the post-refactor layout:

    Current table:
    ```
    | **Send to PC** | 把手机文本框里的所有内容粘贴到 PC 当前焦点窗口，然后清空手机文本框 |
    | **Backspace** | 删除 PC 当前光标前的一个词，不修改手机文本框 |
    | **Enter** | 发送回车键到 PC |
    | **Clear** | 清空 PC 当前焦点输入区域，不修改手机文本框 |
    ```

    Replace with:
    ```
    | 按钮 | 功能 |
    |---|---|
    | **Send to PC** | 把手机文本框里的所有内容发送到 PC，然后清空手机文本框 |
    | **Backspace** | 删除 PC 光标前的一个字符（普通 Backspace，不含修饰键） |
    | **Enter** | 发送回车键到 PC |
    | **Clear text** (本地) | 仅清空手机文本框，不发送任何 HTTP 请求 |
    | **Clear PC field** (远程) | 全选并删除 PC 当前输入区域的所有内容（危险操作，视觉上已区分） |
    ```

    This matches the 2x2 button grid from Phase 2: backspace, Clear text, Clear PC field, enter. The "Clear text" button is a local-only action (no HTTP request), while "Clear PC field" sends SelectAll + Backspace to the PC. Per Phase 2 decisions, "Clear PC field" should be visually distinct (styled as remote/dangerous action) — mention this in the description.

    Also update the tip below the table (about Enter key behavior on phone keyboard) to remove the implication that Enter always sends — note that Shift+Enter inserts a newline locally.
  </action>
  <verify>
    <automated>
      grep -c "Clear text" README.md | xargs test 1 -eq; grep -c "Clear PC field" README.md | xargs test 1 -eq; grep -c "普通 Backspace" README.md | xargs test 1 -eq
    </automated>
  </verify>
  <done>
    "README button table distinguishes Clear text (local) vs Clear PC field (remote/dangerous), Backspace describes single char"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation | README.md is informational only — no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-DOC-01 | Information Disclosure | README.md | accept | Documentation only; no sensitive information is exposed |
| T-03-DOC-SC | Tampering | npm/pip/cargo installs | accept | No new packages added in this plan |
</threat_model>

<verification>
- [ ] Task 1: No Socket.IO / WebSocket / socketioxide references remain in README.md
- [ ] Task 2: README button table has "Clear text" and "Clear PC field" as distinct entries, Backspace described as single char
- [ ] Full README review: `grep -n 'socket.io\|socketioxide\|WebSocket\|Clear \|Backspace' README.md` confirms all changes
</verification>

<success_criteria>
- README architecture diagram shows `axum HTTP + Bearer token` (no Socket.IO)
- README dependencies table no longer lists socketioxide, matches current Cargo.toml
- README button table: "Clear text" (local only) and "Clear PC field" (remote/dangerous) are distinct entries
- README Backspace: described as "普通 Backspace" (single char, no modifier)
- README "与 Python 原版区别": no WebSocket or Socket.IO references
</success_criteria>
