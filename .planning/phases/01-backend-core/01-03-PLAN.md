---
phase: 01-backend-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets.rs
autonomous: true
requirements:
  - FE-01
  - FE-02

must_haves:
  truths:
    - "HTML no longer contains `<script src=\"/sio.min.js\">` tag"
    - "HTML no longer contains `socket.io` client library code (io(), socket.emit(), socket.on())"
    - "HTML still renders the page structure: header, textarea, send button, action buttons, history section"
    - "HTML still has `URLSearchParams(location.search)` token reading"
    - "Existing assets tests are adapted (no more socket.io references in assertions)"
  artifacts:
    - path: "src/assets.rs"
      provides: "HTML without Socket.IO script tag or client code"
      not_contains: "/sio.min.js"
    - path: "src/assets.rs"
      provides: "HTML with unchanged UI structure"
      contains: "send to PC"
    - path: "src/assets.rs"
      provides: "HTML with token reading from URL"
      contains: "URLSearchParams(location.search)"
  key_links:
    - from: "src/assets.rs"
      to: "src/server.rs"
      via: "crate::assets::HTML served by GET / handler"
      pattern: "crate::assets::HTML"
---
<objective>
Remove all Socket.IO client code from the embedded HTML page (FE-01, FE-02).

Purpose: The HTML page no longer loads or uses Socket.IO — no script tag,
no io() connection, no socket.emit/socket.on calls.  The page is now a static
UI shell that will be wired to HTTP fetch calls in Phase 2.
The page remains structurally intact (header, textarea, buttons, history section).

Output: Cleaned HTML in assets.rs with socket.io references removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/assets.rs
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip Socket.IO client code from the HTML string</name>
  <files>src/assets.rs</files>
  <action>
    Remove all Socket.IO related code from the HTML const (FE-01, FE-02):

    1. Remove the `<script src="/sio.min.js">` tag on line 11.
    2. Remove the entire `<script>...</script>` block (lines 296-390) which contains:
       - `const socket = io("/", { query: { token: token } });`
       - `socket.on('connect', ...)`, `socket.on('disconnect', ...)`
       - `socket.emit('type_text', { text })` in sendText()
       - `socket.emit('backspace', null)`, `socket.emit('clear_input', null)`,
         `socket.emit('press_key', { key: 'enter' })`
       - `socket.on('history', ...)` for history rendering
       - All event listener bindings that use socket.emit
    3. Remove the `const params = new URLSearchParams(location.search);` and
       `const token = params.get('token') || '';` lines — token reading from URL
       will be re-added with fetch-based code in Phase 2.  (Keep the structural
       HTML components: header, textarea, button elements, history section.)

    Keep everything ELSE unchanged: the HTML structure, CSS styles, the toast
    element and its CSS, all button elements and their IDs, the history-section
    box and history-list, the `.hidden` class and the `.toast` element.

    After removal:
    - The page has no interactive JS (buttons don't send commands — that is Phase 2)
    - The page is pure HTML+CSS without any JavaScript
    - The `const token` reading code is removed since it was only used for socket.io

    Note: If there is no JS at all, we should also remove the `<script>` tag
    wrapper.  The page becomes pure HTML+CSS for Phase 1.

    Also: remove the `let toastT;` and `function showToast(msg) { ... }` and
    event listeners for sendBtn, input, backBtn, clearBtn, enterBtn, historyList,
    renderHistory() since they depend on socket.io or fetch calls.
  </action>
  <verify>
    <automated>! grep -c '/sio.min.js' src/assets.rs; ! grep -c 'socket.io' src/assets.rs; ! grep -c 'io(' src/assets.rs</automated>
  </verify>
  <done>
    HTML no longer contains /sio.min.js, socket.io(), socket.emit, socket.on;
    page structure (buttons, textarea, history section, styles) is intact
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt assets unit tests</name>
  <files>src/assets.rs</files>
  <action>
    Update the `#[cfg(test)] mod tests` block in assets.rs:

    1. Remove or update `test_html_uses_local_socket_io` — this asserted
       `HTML.contains("/sio.min.js")` which is now false.  Replace with a
       test that verifies the script tag is absent:
       ```rust
       #[test]
       fn test_html_has_no_socket_io_script() {
           assert!(!HTML.contains("/sio.min.js"));
       }
       ```

    2. Update or remove `test_html_contains_type_text_event` — this asserted
       `HTML.contains("type_text")`.  Remove this test since type_text is a
       protocol detail, not present in the static page anymore.

    3. Update or remove `test_html_has_history_event_handler` — this asserted
       `HTML.contains("socket.on('history'")`.  Remove this test since the
       event-based history handler is gone.

    4. Update `test_html_reads_token_from_url` — this asserted
       `HTML.contains("URLSearchParams(location.search)")`.  If we removed that
       code, either remove the test or change it to assert the token reading
       code is not present (depending on whether we keep the JS shell).  If
       the page has no JS at all, remove this test.

    5. Keep all other existing tests: `test_html_has_doctype`,
       `test_html_contains_textarea`, `test_html_does_not_contain_cdn_socket_io`,
       `test_html_does_not_contain_google_fonts`, `test_html_contains_send_button`,
       `test_html_contains_backspace_button`, `test_html_contains_enter_button`,
       `test_html_contains_clear_button`, `test_html_has_history_section`,
       `test_html_has_history_list`, `test_html_history_uses_textcontent`,
       `test_html_closes_properly`, `test_html_does_not_contain_server_injected_token`.

    6. Ensure `cargo test` passes for the assets module.
  </action>
  <verify>
    <automated>cargo test -p type-bridge-rs assets 2>&1</automated>
  </verify>
  <done>
    All assets tests pass; Socket.IO-related assertions removed/replaced
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Removing client-side JS reduces the attack surface —
no socket.io library loaded, no WebSocket/polling transport.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Embedded HTML removal | accept | Removing JS reduces XSS surface; no new code added |
| T-03-SC | Tampering | dependency tree | accept | No packages added or removed |
</threat_model>

<verification>
1. `cargo test` assets tests all pass
2. `grep -c '/sio.min.js' src/assets.rs` returns 0
3. `grep -c 'socket\.io' src/assets.rs` returns 0
4. HTML page structure intact: `grep -c 'send to PC' src/assets.rs` > 0
5. History section exists: `grep -c 'history-section' src/assets.rs` > 0
6. `cargo check` passes (no broken references to removed HTML elements)
</verification>

<success_criteria>
- No script tag loading /sio.min.js
- No socket.io() calls or socket.emit/on calls
- No `const socket = io(...)` or socket event handlers
- Page structure (header, textarea, buttons, history section, styles) preserved
- All assets tests pass
</success_criteria>

<output>
Create `.planning/phases/01-backend-core/01-03-SUMMARY.md` when done.
</output>
